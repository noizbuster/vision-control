{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "shared-ui",
  "sourceRoot": "packages/shared-ui/src",
  "projectType": "library",
  "tags": ["platform:browser", "type:library", "scope:shared-ui"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/shared-ui"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/shared-ui"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/shared-ui"
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["build"],
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/*.tgz"],
      "options": {
        "command": "pnpm pack",
        "cwd": "packages/shared-ui"
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"publish is a no-op stub; gated behind a release ADR (PRD 32.3)\""
      }
    }
  }
}
