# @vision-control/vite-react

Build-tool integration skeleton for @vision-control/vite-react.

> Skeleton generated by `@vision-control/nx-plugin` (node/integration).
> Nx tags: platform:node, type:integration, scope:vite-react.

## Scripts

Run from the repository root:

```bash
pnpm build        # tsc -p tsconfig.build.json -> dist/
pnpm typecheck    # tsc --noEmit -p tsconfig.json
pnpm test         # vitest run
```
