{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "tailwind",
  "sourceRoot": "integrations/tailwind/src",
  "projectType": "library",
  "tags": ["platform:node", "type:integration", "scope:tailwind"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "integrations/tailwind"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "integrations/tailwind"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "integrations/tailwind"
      }
    }
  }
}
