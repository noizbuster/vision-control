{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "overlay-ui",
  "sourceRoot": "packages/overlay-ui/src",
  "projectType": "library",
  "tags": ["platform:browser", "type:library", "scope:overlay-ui"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/overlay-ui"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/overlay-ui"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/overlay-ui"
      }
    }
  }
}
