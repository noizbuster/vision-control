{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "workspace-index",
  "sourceRoot": "packages/workspace-index/src",
  "projectType": "library",
  "tags": ["platform:node", "type:library", "scope:workspace-index"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/workspace-index"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/workspace-index"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/workspace-index"
      }
    }
  }
}
