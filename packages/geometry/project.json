{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "geometry",
  "sourceRoot": "packages/geometry/src",
  "projectType": "library",
  "tags": ["platform:isomorphic", "type:library", "scope:geometry"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/geometry"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/geometry"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/geometry"
      }
    }
  }
}
