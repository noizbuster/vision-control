# @vision-control/change-journal

Isomorphic library skeleton for @vision-control/change-journal.

> Skeleton generated by `@vision-control/nx-plugin` (isomorphic/library).
> Nx tags: platform:isomorphic, type:library, scope:change-journal.

## Scripts

Run from the repository root:

```bash
pnpm build        # tsc -p tsconfig.build.json -> dist/
pnpm typecheck    # tsc --noEmit -p tsconfig.json
pnpm test         # vitest run
```
