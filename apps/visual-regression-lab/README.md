# @vision-control/visual-regression-lab

Test-fixture app skeleton for @vision-control/visual-regression-lab.

> Skeleton generated by `@vision-control/nx-plugin` (browser/fixture).
> Nx tags: platform:browser, type:fixture, scope:visual-regression-lab.

## Scripts

Run from the repository root:

```bash
pnpm build        # tsc -p tsconfig.build.json -> dist/
pnpm typecheck    # tsc --noEmit -p tsconfig.json
pnpm test         # vitest run
```
