{
  "$comment": "OpenCode MCP config for the stdio transport. Drop this into your opencode.json (project or user config). No VC_DAEMON_URL: the MCP process is an optional projection bridge (ADR-020). Pair the extension on loopback :4322 for live data. The server is read-only: no source-writing tool exists.",
  "mcp": {
    "vision-control": {
      "type": "local",
      "command": ["pnpm", "exec", "vision-control-mcp"],
      "enabled": true
    }
  }
}
