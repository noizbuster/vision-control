{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "vanilla-css",
  "sourceRoot": "integrations/vanilla-css/src",
  "projectType": "library",
  "tags": ["platform:node", "type:integration", "scope:vanilla-css"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "integrations/vanilla-css"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "integrations/vanilla-css"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "integrations/vanilla-css"
      }
    }
  }
}
