{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "next-react",
  "sourceRoot": "integrations/next-react/src",
  "projectType": "library",
  "tags": ["platform:node", "type:integration", "scope:next-react"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "integrations/next-react"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "integrations/next-react"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "integrations/next-react"
      }
    }
  }
}
