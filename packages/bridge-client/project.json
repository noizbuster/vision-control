{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "bridge-client",
  "sourceRoot": "packages/bridge-client/src",
  "projectType": "library",
  "tags": ["platform:isomorphic", "type:library", "scope:bridge-client"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/bridge-client"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/bridge-client"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/bridge-client"
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["build"],
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/*.tgz"],
      "options": {
        "command": "pnpm pack",
        "cwd": "packages/bridge-client"
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"publish is a no-op stub; gated behind a release ADR (PRD 32.3)\""
      }
    }
  }
}
