{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "preview-engine",
  "sourceRoot": "packages/preview-engine/src",
  "projectType": "library",
  "tags": ["platform:isomorphic", "type:library", "scope:preview-engine"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/preview-engine"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/preview-engine"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/preview-engine"
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["build"],
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/*.tgz"],
      "options": {
        "command": "pnpm pack",
        "cwd": "packages/preview-engine"
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"publish is a no-op stub; gated behind a release ADR (PRD 32.3)\""
      }
    }
  }
}
