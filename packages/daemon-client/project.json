{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "daemon-client",
  "sourceRoot": "packages/daemon-client/src",
  "projectType": "library",
  "tags": ["platform:isomorphic", "type:library", "scope:daemon-client"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/daemon-client"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/daemon-client"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/daemon-client"
      }
    }
  }
}
