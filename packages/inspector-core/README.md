# @vision-control/inspector-core

Browser library skeleton for @vision-control/inspector-core.

> Skeleton generated by `@vision-control/nx-plugin` (browser/library).
> Nx tags: platform:browser, type:library, scope:inspector-core.

## Scripts

Run from the repository root:

```bash
pnpm build        # tsc -p tsconfig.build.json -> dist/
pnpm typecheck    # tsc --noEmit -p tsconfig.json
pnpm test         # vitest run
```
