{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "pi",
  "sourceRoot": "integrations/pi/src",
  "projectType": "library",
  "tags": ["platform:node", "type:integration", "scope:pi"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "integrations/pi"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "integrations/pi"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "integrations/pi"
      }
    }
  }
}
