{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "change-journal",
  "sourceRoot": "packages/change-journal/src",
  "projectType": "library",
  "tags": ["platform:isomorphic", "type:library", "scope:change-journal"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/change-journal"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/change-journal"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/change-journal"
      }
    }
  }
}
