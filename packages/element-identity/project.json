{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "element-identity",
  "sourceRoot": "packages/element-identity/src",
  "projectType": "library",
  "tags": ["platform:isomorphic", "type:library", "scope:element-identity"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/element-identity"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/element-identity"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/element-identity"
      }
    }
  }
}
