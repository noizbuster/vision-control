{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "css-modules",
  "sourceRoot": "integrations/css-modules/src",
  "projectType": "library",
  "tags": ["platform:node", "type:integration", "scope:css-modules"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "integrations/css-modules"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "integrations/css-modules"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "integrations/css-modules"
      }
    }
  }
}
