{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "layout-engine",
  "sourceRoot": "packages/layout-engine/src",
  "projectType": "library",
  "tags": ["platform:isomorphic", "type:library", "scope:layout-engine"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/layout-engine"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/layout-engine"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/layout-engine"
      }
    }
  }
}
