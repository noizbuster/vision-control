{
  "$comment": "Pi MCP server entry for the stdio transport. Map these fields onto Pi's current MCP client settings. The command spawns the vision-control-mcp binary as a child process; Pi talks JSON-RPC over stdin/stdout. No VC_DAEMON_URL: pair the extension on loopback :4322 for live data. The server is read-only: no source-writing tool exists.",
  "servers": {
    "vision-control": {
      "transport": "stdio",
      "command": ["pnpm", "exec", "vision-control-mcp"]
    }
  }
}
