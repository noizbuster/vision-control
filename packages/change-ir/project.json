{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "change-ir",
  "sourceRoot": "packages/change-ir/src",
  "projectType": "library",
  "tags": ["platform:isomorphic", "type:library", "scope:change-ir"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/change-ir"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/change-ir"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/change-ir"
      }
    }
  }
}
