{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "playground-next",
  "sourceRoot": "apps/playground-next/src",
  "projectType": "library",
  "tags": ["platform:browser", "type:fixture", "scope:playground-next"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "apps/playground-next"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "apps/playground-next"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "apps/playground-next"
      }
    }
  }
}
