{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "verification-engine",
  "sourceRoot": "packages/verification-engine/src",
  "projectType": "library",
  "tags": ["platform:isomorphic", "type:library", "scope:verification-engine"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/verification-engine"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "packages/verification-engine"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/verification-engine"
      }
    }
  }
}
