{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "svelte",
  "sourceRoot": "integrations/svelte/src",
  "projectType": "library",
  "tags": ["platform:node", "type:integration", "scope:svelte"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "integrations/svelte"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "integrations/svelte"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "integrations/svelte"
      }
    }
  }
}
