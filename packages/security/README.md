# @vision-control/security

Isomorphic library skeleton for @vision-control/security.

> Skeleton generated by `@vision-control/nx-plugin` (isomorphic/library).
> Nx tags: platform:isomorphic, type:library, scope:security.

## Scripts

Run from the repository root:

```bash
pnpm build        # tsc -p tsconfig.build.json -> dist/
pnpm typecheck    # tsc --noEmit -p tsconfig.json
pnpm test         # vitest run
```
