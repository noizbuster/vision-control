# @vision-control/playground-react-vite

Test-fixture app skeleton for @vision-control/playground-react-vite.

> Skeleton generated by `@vision-control/nx-plugin` (browser/fixture).
> Nx tags: platform:browser, type:fixture, scope:playground-react-vite.

## Scripts

Run from the repository root:

```bash
pnpm build        # tsc -p tsconfig.build.json -> dist/
pnpm typecheck    # tsc --noEmit -p tsconfig.json
pnpm test         # vitest run
```
