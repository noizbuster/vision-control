{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "opencode",
  "sourceRoot": "integrations/opencode/src",
  "projectType": "library",
  "tags": ["platform:node", "type:integration", "scope:opencode"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "integrations/opencode"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "integrations/opencode"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "integrations/opencode"
      }
    }
  }
}
