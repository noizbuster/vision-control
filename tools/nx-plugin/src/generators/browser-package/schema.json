{
  "$schema": "http://json-schema.org/schema",
  "title": "Browser Package Generator",
  "description": "Scaffold a browser library or app (platform:browser). Defaults to type:library; pass type:'app' for a browser app.",
  "type": "object",
  "properties": {
    "name": { "type": "string", "$default": { "$source": "argv", "index": 0 } },
    "directory": { "type": "string", "alias": "d" },
    "description": { "type": "string" },
    "scope": { "type": "string" },
    "type": { "type": "string", "enum": ["library", "app"], "default": "library" }
  },
  "required": ["name", "directory"]
}
